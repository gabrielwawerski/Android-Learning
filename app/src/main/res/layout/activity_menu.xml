<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="masmix1.com.myapps.MenuActivity">

    <TextView
        android:id="@+id/menu_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:layout_marginTop="20dp"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        android:text="@string/menu_title" />

    <EditText
        android:id="@+id/send_to_next_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/text_for_next_activity"
        android:layout_below="@id/menu_title"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:textSize="12sp"
        android:maxLines="1"
        android:maxHeight="12dp"/>

    <Button
        android:id="@+id/piwnik"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/send_to_next_activity"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:text="@string/piwnik_name"
        android:onClick="onClickStartPiwnik" />

    <Button
        android:id="@+id/send_sms"
        android:layout_height="wrap_content"
        android:layout_below="@id/piwnik"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"
        android:text="@string/send_sms_name"
        android:onClick="onClickStartSendSms"
        android:layout_width="120dp" />

    <Button
        android:id="@+id/thread"
        android:layout_height="wrap_content"
        android:layout_below="@id/send_sms"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"
        android:text="@string/thread_title"
        android:onClick="onClickStartThread"
        android:layout_width="120dp" />

    <Button
        android:id="@+id/timer"
        android:layout_height="wrap_content"
        android:layout_width="120dp"
        android:layout_below="@id/thread"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"
        android:text="@string/timerTitle"
        android:onClick="onClickStartTimer" />

</RelativeLayout>
